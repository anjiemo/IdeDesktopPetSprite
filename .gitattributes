# 行尾策略：仓库内统一规范化为 LF；检出到工作区时按平台原生换行符
#   Windows → CRLF，macOS / Linux → LF（依赖 core.eol=native 默认值，无需额外配置）
* text=auto

# 二进制文件：不做换行转换，也不当作文本 diff
*.png binary
*.jar binary

# Unix 启动脚本在所有平台都必须是 LF
gradlew text eol=lf
